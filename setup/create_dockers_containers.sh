#!/usr/bin/env bash

docker build -f .Dockerfiles/DockerFile .
#

# USE A DNS SERVER
# 




# Create Master Server
# Start Master Server
# Get the ip of the master server
# Connect with ssh to all the remote edges
# Start all the remote edges
# Start the cloud server
# Start the controller


